# ── Prompt layout ───────────────────────────────────────────────
format = """
[](fg:#1e1e2e)\
$os\
$username\
[](bg:#313244 fg:#1e1e2e)\
$directory\
[](fg:#313244 bg:#45475a)\
$git_branch\
$git_status\
[](fg:#45475a bg:#1e1e2e)\
$nodejs\
$python\
$rust\
$golang\
$java\
$kotlin\
$php\
$c\
$cmake\
$dart\
$dotnet\
$elixir\
$haskell\
$lua\
$perl\
$ruby\
$scala\
$swift\
$zig\
$docker_context\
[](fg:#1e1e2e)\
$fill\
$cmd_duration\
$time\
$line_break\
$character"""

# ── Right-side fill spacer ───────────────────────────────────────
[fill]
symbol = " "

# ── OS icon ─────────────────────────────────────────────────────
[os]
disabled = false
style = "bg:#1e1e2e fg:#cba6f7"    # mauve

[os.symbols]
Alpine = " "
Android = " "
Arch = "󰣇 "
Artix = " "
CachyOS = "󰣇 "
CentOS = " "
Debian = "󰣚 "
EndeavourOS = " "
Fedora = "󰣛 "
FreeBSD = " "
Garuda = " "
Gentoo = "󰣨 "
Kali = " "
Linux = " "
Macos = " "
Manjaro = " "
MidnightBSD = " "
Mint = "󰣭 "
NetBSD = " "
NixOS = " "
OpenBSD = " "
openSUSE = " "
OracleLinux = "󰌷 "
PikaOS = "󱗆 "
Pop = " "
Raspbian = "󰐿 "
Redhat = " "
RedHatEnterprise = " "
Redox = "󰀘 "
RockyLinux = " "
Solus = " "
SUSE = " "
Ubuntu = " "
Unknown = "󰀶 "
Void = " "
Windows = "󰍲 "

# ── Username ─────────────────────────────────────────────────────
[username]
show_always  = true
style_user   = "bg:#1e1e2e fg:#89b4fa bold"   # blue
style_root   = "bg:#1e1e2e fg:#f38ba8 bold"   # red (root warning)
format       = '[ $user ]($style)'

# ── Directory ────────────────────────────────────────────────────
[directory]
style            = "bg:#313244 fg:#cba6f7 bold"   # mauve
format           = "[ 󰉋 $path ]($style)"
truncation_length = 3
truncate_to_repo = true
read_only        = " 󰌾"
read_only_style  = "bg:#313244 fg:#f38ba8"

[directory.substitutions]
"~"         = "󰜥 "
"Documents" = "󰈙 "
"Downloads" = "󰇚 "
"Music"     = "󰝚 "
"Pictures"  = "󰋩 "
"Videos"    = "󰕧 "
"Desktop"   = "󰖲 "
"Developer" = "󰲋 "
".config"   = " "

# ── Git branch ───────────────────────────────────────────────────
[git_branch]
style    = "bg:#45475a fg:#f5c2e7 bold"   # pink
format   = '[ $symbol$branch(:$remote_branch) ]($style)'
symbol   = " "

# ── Git status ───────────────────────────────────────────────────
[git_status]
style     = "bg:#45475a fg:#fab387"   # peach
format    = '([$all_status$ahead_behind ]($style))'
conflicted = "󰞇 "
ahead      = "⇡${count} "
behind     = "⇣${count} "
diverged   = "⇕⇡${ahead_count}⇣${behind_count} "
up_to_date = ""
untracked  = "? "
stashed    = "󰏢 "
modified   = "! "
staged     = "+ "
renamed    = "» "
deleted    = "✘ "

# ── Language versions ────────────────────────────────────────────
[nodejs]
style   = "bg:#1e1e2e fg:#cba6f7"   # mauve
format  = '[ $symbol($version) ]($style)'
symbol  = " "

[python]
style         = "bg:#1e1e2e fg:#f9e2af"   # yellow
format        = '[ $symbol($version)(\($virtualenv\)) ]($style)'
symbol        = " "
python_binary = ["python3", "python"]

[rust]
style  = "bg:#1e1e2e fg:#fab387"   # peach
format = '[ $symbol($version) ]($style)'
symbol = " "

[golang]
style  = "bg:#1e1e2e fg:#89dceb"   # sky
format = '[ $symbol($version) ]($style)'
symbol = " "

[java]
style  = "bg:#1e1e2e fg:#eba0ac"   # maroon
format = '[ $symbol($version) ]($style)'
symbol = " "

[kotlin]
style  = "bg:#1e1e2e fg:#cba6f7"   # mauve
format = '[ $symbol($version) ]($style)'
symbol = " "

[php]
style  = "bg:#1e1e2e fg:#b4befe"   # lavender
format = '[ $symbol($version) ]($style)'
symbol = " "

[c]
style  = "bg:#1e1e2e fg:#89b4fa"   # blue
format = '[ $symbol($version) ]($style)'
symbol = " "

[cmake]
style  = "bg:#1e1e2e fg:#a6e3a1"   # green
format = '[ $symbol($version) ]($style)'
symbol = "△ "

[dart]
style  = "bg:#1e1e2e fg:#94e2d5"   # teal
format = '[ $symbol($version) ]($style)'
symbol = " "

[dotnet]
style  = "bg:#1e1e2e fg:#cba6f7"   # mauve
format = '[ $symbol($version) ]($style)'
symbol = "󰪮 "

[elixir]
style  = "bg:#1e1e2e fg:#b4befe"   # lavender
format = '[ $symbol($version) ]($style)'
symbol = " "

[haskell]
style  = "bg:#1e1e2e fg:#eba0ac"   # maroon
format = '[ $symbol($version) ]($style)'
symbol = " "

[lua]
style  = "bg:#1e1e2e fg:#89b4fa"   # blue
format = '[ $symbol($version) ]($style)'
symbol = " "

[perl]
style  = "bg:#1e1e2e fg:#89dceb"   # sky
format = '[ $symbol($version) ]($style)'
symbol = " "

[ruby]
style  = "bg:#1e1e2e fg:#f38ba8"   # red
format = '[ $symbol($version) ]($style)'
symbol = " "

[scala]
style  = "bg:#1e1e2e fg:#f38ba8"   # red
format = '[ $symbol($version) ]($style)'
symbol = " "

[swift]
style  = "bg:#1e1e2e fg:#fab387"   # peach
format = '[ $symbol($version) ]($style)'
symbol = " "

[zig]
style  = "bg:#1e1e2e fg:#f9e2af"   # yellow
format = '[ $symbol($version) ]($style)'
symbol = " "

[docker_context]
style  = "bg:#1e1e2e fg:#74c7ec"   # sapphire
format = '[ $symbol$context ]($style)'
symbol = " "

# ── Command duration ─────────────────────────────────────────────
[cmd_duration]
style             = "fg:#7f849c italic"   # overlay1
format            = '[ 󱑂 $duration ]($style)'
min_time          = 2_000
show_milliseconds = false

# ── Time ─────────────────────────────────────────────────────────
[time]
disabled      = false
style         = "fg:#585b70"   # surface2
format        = '[ 󱑃 $time ]($style)'
time_format   = "%H:%M"
use_12hr      = false

# ── Prompt character ─────────────────────────────────────────────
[character]
success_symbol = "[❯](bold fg:#cba6f7)"   # mauve on success
error_symbol   = "[❯](bold fg:#f38ba8)"   # red on error
vimcmd_symbol  = "[❮](bold fg:#cba6f7)"   # mauve in vim mode

# ── Modules to disable (noise reduction) ────────────────────────
[package]
disabled = true

[aws]
disabled = true

[gcloud]
disabled = true
